from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Predict popularity of a track (in Singapore) based on the track's attributes',
    author='Alexandra (thedatacurious)',
    license='',
)
